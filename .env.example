# Database
# For local development with Docker:
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/thinking_partner
# For Railway: Use the DATABASE_URL provided by Railway PostgreSQL plugin

# AI APIs (Required)
ANTHROPIC_API_KEY=sk-ant-your-key-here
OPENAI_API_KEY=sk-your-openai-key-here

# Optional: Deepgram for real-time streaming (if you prefer over Whisper)
# DEEPGRAM_API_KEY=your-deepgram-key-here

# Server
PORT=8000
HOST=0.0.0.0

# Transcription settings
# Options: "whisper" (default, recommended) or "deepgram"
TRANSCRIPTION_PROVIDER=whisper

# Pause detection (milliseconds)
PAUSE_THRESHOLD_MS=2000
